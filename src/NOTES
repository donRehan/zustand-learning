Add the object right use the example I created
Access the value right again using the example. I was too off

If that don't work set an example after reading the next docs for zustand
and then comeback again
